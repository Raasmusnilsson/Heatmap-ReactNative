import { StyleSheet } from 'react-native'

export const LocationButtonStyles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        position: 'absolute',
        bottom: 36,
        left: 43,
    },
})
